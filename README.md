# icp-fhir

## local dev dependencies

none, simply run the IG publisher with the following command:

```bash
java -jar org.hl7.fhir.publisher.cli.jar -ig .

%JAVA_HOME%\bin\java -jar ../sushi/fhir-ig-publisher/publisher.jar -ig .
```

## tbi branch to be merged to trunk when ready
